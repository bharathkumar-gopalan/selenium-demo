package com.flaconi.ecomm.portal.qa.automation.environment;

/**
 * An enum representing the current working environment , Environment is usually
 * a collection of endpoints or URLs which the automation has to access in the
 * current working context
 * 
 * Environment selection is done by the environment handler
 * 
 * @author bharath
 *
 */
public enum Environment {
	/*
	 * More environments are possible , QA , BETA etc etc , Currently we have
	 * only flaconi prod
	 */
	PROD;

}
